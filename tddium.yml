---
:tddium:
  :tests:
    - rubocop
  :ruby_version: ruby-2.0.0
  :bundler_version: 1.3.5
  :postgresql:
    :version: '9.3'
  :rake:
    :rails_env: test
  :hooks:
    :worker_setup: bundle exec rake db:test:prepare
    :post_build: 'git push git@heroku.com:playround-staging.git master && heroku run rake db:migrate --app playround-staging'
